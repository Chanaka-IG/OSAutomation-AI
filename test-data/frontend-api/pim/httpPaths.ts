/**
 * OrangeHRM PIM REST routes used by UI/API tests.
 * Mirror of `test-data/api/employees.adminPath` — kept here so tests never depend on master seed modules for URLs.
 */
export const employeesAdminPath = '/web/index.php/api/v2/pim/employees';
