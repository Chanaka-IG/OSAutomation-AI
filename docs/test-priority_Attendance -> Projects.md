# Test Priority — Attendance → Projects

> Input: `docs/test-strategy_Attendance -> Projects.md`. Feature: **Time → Project Info → Projects**.
> Scope: **E2E scenarios only** (API-primary TC-103/104/106/202/400/402 owned by the API layer).
> Generated by `/define-priority`. Consumed by `/generate-tests`.

Priority key: **P0** release-blocking · **P1** high impact · **P2** moderate · **P3** low/cosmetic.

---

## P0 — Release-blocking (core CRUD, security, no workaround)

| ID | Scenario | Rationale |
|---|---|---|
| TC-001 | Add a project with a name + existing customer | The core create flow; the feature exists to define projects. No workaround. |
| TC-002 | List shows Project + Customer Name columns | The core read surface; admins must see the project catalog. |
| TC-004 | Delete a project via trash + confirm | Core destructive op; must be guarded and effective. |
| TC-200 | ESS user cannot access Projects | Security/authorization — Project Info is privileged. |
| TC-201 | Unauthenticated access redirects to login | Security baseline for the route. |

## P1 — High impact (primary path, validation, high reach)

| ID | Scenario | Rationale |
|---|---|---|
| TC-003 | Edit a project (rename) persists | Core update; common admin action. |
| TC-005 | Add a customer inline via the modal | The customer is a hard dependency of every project; the inline flow is the primary way to create one mid-add. |
| TC-100 | Name is required | Primary validation guard on the add form. |
| TC-101 | Customer is required | The project↔customer invariant enforced in the UI. |
| TC-303 | Duplicate project name is blocked | Data-integrity validation; prevents ambiguous catalogs. |
| TC-007 | Search the list by name | The list's main control once projects accumulate. |
| TC-504 | Success toast on save | The admin's confirmation the save took. |
| TC-501 | "(N) Records Found" reflects the count | Roster-count integrity. |

## P2 — Moderate (secondary flows, overlaps, edge-on-common-path)

| ID | Scenario | Rationale |
|---|---|---|
| TC-006 | Add a project with a Project Admin | Optional secondary field. |
| TC-008 | Add an activity to a project | Sub-feature; valuable but secondary to project CRUD. |
| TC-102 | Duplicate name (business-rule view) | Overlaps TC-303. |
| TC-105 | Multiple project admins add/remove | Form list editing; less common. |
| TC-203 | ESS add-form deep link privileged | Reinforces TC-200. |
| TC-300 | Empty Name → Required | Overlaps TC-100. |
| TC-301 | Empty Customer → Required | Overlaps TC-101. |
| TC-302 | Whitespace-only Name → Required | Trim edge. |
| TC-401 | XSS payload in name stored inert | Security hardening; lower frequency. |
| TC-500 | Empty list "No Records Found" | Empty-state render. |
| TC-502 | Required markers + "* Required" | Affordance detail. |
| TC-503 | Delete confirmation dialog copy | Confirm-dialog detail. |
| TC-505 | Edit page Activities section | Sub-section presence. |
| TC-506 | Search filter panel toggle | Secondary UI interaction. |

## P3 — Low / cosmetic

| ID | Scenario | Rationale |
|---|---|---|
| TC-403 | Add/remove multiple admins before save | Rare form-editing edge. |
| TC-404 | Duplicate customer name | Edge on the secondary (customer) entity. |

---

## Generation Scope (handoff to `/generate-tests`)

- **P0 + P1 = 13 scenarios**, which is **> 12** → the suite covers **P0 and P1 only** (P2/P3 deferred).
  No unit/integration tests are generated.
- **P0 (5):** TC-001, TC-002, TC-004, TC-200, TC-201
- **P1 (8):** TC-003, TC-005, TC-100, TC-101, TC-303, TC-007, TC-504, TC-501
- One self-contained E2E test **per scenario** (no folding), naming `TC-TIME-PRJ-<id>`.

### Notes for generation
- Add a `ProjectsPage` (list: rows, Add, search, row delete/edit; add/edit form: Name, Customer autocomplete +
  inline Add Customer modal, Description, Project Admin, Save) and a `TimeProjectsApi` (create/list/delete
  projects + customers).
- **A project requires a customer** → seed a customer via API in `beforeAll`; seed a project for read/edit/
  delete/search/duplicate tests. Use **unique names** (`Date.now()`); **hard-delete created projects (and the
  seeded customer) in `afterAll`** via `DELETE /time/projects {ids}` / `…/customers {ids}`.
- Assert by **unique name** / **relative count** — never absolute totals (the instance already carries a
  leftover "PIO Explore Customer").
- ESS tests use `auth.essTestUser` (`marcus.chen`) → "Credential Required". Confirm the duplicate-name copy
  ("Already exists") live during generation.
- Project save redirects to `/time/saveProject/{id}`; toast "Successfully Saved".
